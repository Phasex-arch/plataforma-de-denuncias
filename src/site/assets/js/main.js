// Inicializar a página
document.addEventListener('DOMContentLoaded', function() {
    // Inicializar o toggle do formulário
    toggleTipoInstituicao();
    
    // Outras inicializações podem ser adicionadas aqui
});